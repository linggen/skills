{
  "$schema_version": 3,
  "$comment": "Pulse config. `workspace_path` points at the user's product source dir; the agent reads README/doc/source there on every run for product knowledge (read-only, just-in-time via Read/Glob/Grep). `default_goal` is used when a run is triggered without an explicit goal. `sites` toggles source tools + per-site config. `targets` toggles output lanes + per-lane config. Goals dispatch capabilities; capabilities call enabled sites and draft for enabled targets. Reddit is dual-role.",
  "workspace_path": "",
  "default_goal": "Daily X-post if I shipped or learned something yesterday. Skip if nothing meaningful.",
  "sites": {
    "hackernews": {
      "enabled": true
    },
    "reddit": {
      "enabled": true,
      "subs": ["LocalLLaMA", "macapps"]
    },
    "lobsters": {
      "enabled": false
    },
    "arxiv": {
      "enabled": false
    },
    "rss": {
      "enabled": false,
      "feeds": []
    },
    "google-trends": {
      "enabled": false,
      "region": "US"
    },
    "github-trending": {
      "enabled": false,
      "language": ""
    },
    "product-hunt": {
      "enabled": false
    },
    "wikipedia-pageviews": {
      "enabled": false,
      "topics": []
    }
  },
  "targets": {
    "x-post": { "enabled": true },
    "reddit-comment": { "enabled": false },
    "medium": { "enabled": false },
    "blog": { "enabled": true, "min_words": 800, "max_words": 2000 },
    "linkedin": { "enabled": false },
    "substack": { "enabled": false }
  }
}
